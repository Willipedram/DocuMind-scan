# راهنمای استفاده از DocuMind Scan

این راهنما مراحل کار با برنامه را به‌صورت عملی توضیح می‌دهد.

## 0) نصب پیش‌نیازها (خیلی مهم)

### ساخت محیط مجازی
```bash
python -m venv .venv
# Windows
.venv\Scripts\activate
# Linux/macOS
source .venv/bin/activate
```

### نصب کتابخانه‌های Python
```bash
pip install --upgrade pip
pip install pyside6 opencv-python easyocr pandas openpyxl pillow pdf2image pypdfium2 numpy
```

### نصب Poppler برای PDF (اجباری برای pdf2image)
اگر این بخش نصب نباشد، خطای `Unable to load PDF` می‌گیرید.
در این پروژه fallback هم گذاشته شده و با `pypdfium2` هم می‌تواند PDF را بخواند.

**Windows (پیشنهادی با Chocolatey):**
```powershell
choco install poppler
```

**Linux (Debian/Ubuntu):**
```bash
sudo apt-get update
sudo apt-get install -y poppler-utils
```

**macOS (Homebrew):**
```bash
brew install poppler
```


### اگر هنوز PDF خطا می‌دهد (با وجود نصب pypdfium2)
احتمالاً برنامه با یک Python دیگر اجرا می‌شود. این دو دستور را در همان ترمینالی که برنامه را اجرا می‌کنید بزنید:

```bash
python -c "import sys; print(sys.executable)"
python -c "import pypdfium2; print('pypdfium2 OK', pypdfium2.__version__)"
```

اگر دستور دوم خطا داد، یعنی `pypdfium2` روی همان Python نصب نشده است.

### تنظیم POPPLER_PATH (در صورت نیاز در ویندوز)
اگر Poppler در PATH سیستم نیست:
```powershell
setx POPPLER_PATH "C:\path\to\poppler\Library\bin"
```

---

## 1) اجرای برنامه
1. وابستگی‌ها را نصب کنید (Python 3.10+).
2. برنامه را اجرا کنید:

```bash
python main.py
```

## 2) بارگذاری فایل
- دکمه `+ وارد کردن فایل` را بزنید و یکی از فرمت‌های `PDF / JPG / PNG` را انتخاب کنید.
- برای چند فایل، از `+ افزودن دسته‌ای` استفاده کنید.

## 3) پیش‌نمایش سند
- در بخش وسط، سند نمایش داده می‌شود.
- با دکمه‌های `صفحه قبل / صفحه بعد` بین صفحات PDF جابه‌جا شوید.
- با `+` و `-` زوم را تنظیم کنید.

## 4) اجرای OCR
- روی `OCR + تشخیص فیلد` کلیک کنید.
- متن استخراج‌شده و فیلدهای تشخیص‌داده‌شده در پنل راست نمایش داده می‌شوند.

## 5) انتخاب ستون‌های خروجی
- در جدول فیلدها:
  - با چک‌باکس مشخص کنید کدام فیلدها خروجی بگیرند.
  - نام ستون Excel را در ستون `نام ستون Excel` ویرایش کنید.

## 6) ذخیره/بارگذاری قالب
- `ذخیره قالب`: تنظیمات ستون‌های انتخابی را به‌صورت JSON ذخیره می‌کند.
- `بارگذاری قالب`: یک قالب ذخیره‌شده را روی جدول اعمال می‌کند.
- اگر فایل جدیدی وارد کنید و قبلاً قالب متناظر داشته باشید، برنامه از روی نام فایل/نوع سند قالب را خودکار اعمال می‌کند تا سرعت کار بالا برود.
- اگر هنگام OCR قالب مشابه پیدا شود، برنامه آن را خودکار اعمال می‌کند.

## 7) خروجی Excel
- روی `خروجی Excel` کلیک کنید.
- مسیر فایل `.xlsx` را انتخاب کنید.
- هر سند به‌عنوان یک ردیف ذخیره می‌شود و ستون‌ها از انتخاب شما ساخته می‌شوند.

## 8) پردازش دسته‌ای
- ابتدا چند فایل را با `+ افزودن دسته‌ای` انتخاب کنید.
- سپس `پردازش دسته‌ای` را بزنید.
- نوار پیشرفت، ETA و وضعیت هر فایل (`pending/success/error`) نمایش داده می‌شود.
- خطای یک فایل باعث توقف کل پردازش نمی‌شود.

## 9) برچسب‌گذاری دستی و یادگیری
- روی سند درگ کنید تا یک ناحیه انتخاب شود.
- `برچسب‌گذاری ناحیه` را بزنید و نام برچسب وارد کنید.
- نمونه برچسب ذخیره می‌شود و مدل سبک به‌صورت افزایشی به‌روزرسانی می‌شود.
- با `پیش‌بینی جایگاه فیلد` می‌توانید موقعیت احتمالی همان برچسب را روی سند جدید ببینید.

## 10) لاگ‌ها
لاگ‌های ترمینال با این برچسب‌ها نمایش داده می‌شوند:
- `[INFO]`
- `[PROCESS]`
- `[SUCCESS]`
- `[ERROR]`

## رفع خطای PDF
اگر خطای `Unable to load PDF` دیدید:
1. Poppler را نصب کنید.
2. برنامه را کامل ببندید و دوباره اجرا کنید.
3. در ویندوز، اگر لازم بود `POPPLER_PATH` تنظیم کنید.
