# Fonts

Place Vazirmatn font files in this directory for Persian UI rendering:

- `Vazirmatn-Regular.ttf` (preferred)
- or `Vazirmatn[wght].ttf`

The application will load these files automatically if present.
